# E-commerce - Controle de Estoque

Project made using [Create React App](https://github.com/facebook/create-react-app).

## Available Scripts

In the project directory, first run:

### `npm i`

Then run:

### `npm start`

Launches the app in localhost
